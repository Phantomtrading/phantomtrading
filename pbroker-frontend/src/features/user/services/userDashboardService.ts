// import { fetchUserDashboardData, type UserDashboardResponse } from '../api/userApi';

export const getUserDashboardData = async (): Promise<any> => {
  try {
    return ({});
  } catch (error) {
    throw error;
  }
}; 